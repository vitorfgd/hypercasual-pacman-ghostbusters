/** Overlap radius (XZ) — must match `DepositController` session test. */
export const TRASH_PORTAL_ZONE_RADIUS = 1.22

/** Pull toward portal when carrying items (world units / sec at rim). */
export const TRASH_PORTAL_SUCTION_MAX_SPEED = 3.85

/** Extra credits per item after the first in one portal session (continuous deposit). */
export const TRASH_PORTAL_COMBO_PER_CHAIN = 2

/** Max combo bonus per single item (value + combo capped display-side in Game). */
export const TRASH_PORTAL_COMBO_MAX = 8
