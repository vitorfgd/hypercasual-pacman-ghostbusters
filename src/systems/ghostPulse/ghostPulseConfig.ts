/**
 * Ghost pulse power: charge bar fills outside the safe room; hold a button to spend charge
 * (ghosts flee). Fill / drain rates scale with upgrades (see `upgradeConfig.ts`).
 */

/** Slight speed boost while pulse power is active. */
export const GHOST_PULSE_SPEED_MULTIPLIER = 1.12
